name: Vérification DNS & HTTP des domaines

on:
  workflow_dispatch:
    inputs:
      prefixes:
        description: "Préfixes des domaines à tester (ex: a, abc, mn)"
        required: true
        default: "a"

  schedule:
    - cron: '0 1 * * *'
    - cron: '0 2 * * *'
    - cron: '0 3 * * *'
    - cron: '0 4 * * *'
    - cron: '0 5 * * *'
    - cron: '0 6 * * *'
    - cron: '0 7 * * *'
    - cron: '0 8 * * *'
    - cron: '0 9 * * *'
    - cron: '0 10 * * *'
    - cron: '0 11 * * *'
    - cron: '0 12 * * *'
    

permissions:
  contents: write  # 🔐 Autorise les pushs vers le dépôt

jobs:
  run-check:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: 🐍 Installer Python et les dépendances
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Installer les modules requis
        run: |
          python -m pip install --upgrade pip
          pip install dnspython httpx

      - name: 🧮 Déterminer les préfixes
        id: set-prefixes
        run: |
          if [[ "${{ github.event_name }}" == "schedule" ]]; then
            hour=$(date -u +"%H")
            case "$hour" in
              "01") echo "value=0123456789" >> $GITHUB_OUTPUT ;;
              "02") echo "value=a" >> $GITHUB_OUTPUT ;;
              "03") echo "value=b" >> $GITHUB_OUTPUT ;;
              "04") echo "value=c" >> $GITHUB_OUTPUT ;;
              "05") echo "value=b" >> $GITHUB_OUTPUT ;;
              "06") echo "value=e" >> $GITHUB_OUTPUT ;;
              "07") echo "value=f" >> $GITHUB_OUTPUT ;;
              "08") echo "value=g" >> $GITHUB_OUTPUT ;;
              "09") echo "value=h" >> $GITHUB_OUTPUT ;;
              "10") echo "value=i" >> $GITHUB_OUTPUT ;;
              "11") echo "value=j" >> $GITHUB_OUTPUT ;;
              "12") echo "value=k" >> $GITHUB_OUTPUT ;; 
            esac
          else
            echo "value=${{ github.event.inputs.prefixes }}" >> $GITHUB_OUTPUT
          fi

      - name: 🚀 Lancer le script de vérification
        run: |
          PREFIXES="${{ steps.set-prefixes.outputs.value }}"
          echo "Préfixes utilisés : $PREFIXES"
          if [ -n "$PREFIXES" ]; then
            python dns_checker.py "$PREFIXES"
          else
            echo "⏭️ Aucun préfixe à exécuter pour cette heure."
          fi

      - name: 📌 Commit et push de dead.txt
        if: success()
        run: |
          if [ -f dead.txt ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add dead.txt
            if git diff --cached --quiet; then
              echo "✅ Aucun changement dans dead.txt à commiter."
            else
              git commit -m "🔄 Mise à jour de dead.txt (préfixes: ${{ steps.set-prefixes.outputs.value }})"
              git push
              echo "✅ dead.txt mis à jour et poussé."
            fi
          else
            echo "❌ Fichier dead.txt non trouvé. Rien à commiter."
          fi
